package com.automacropro.model;

/**
 * Whether the Macro Sequencer plays its step list once, or repeats it forever
 * until stopped manually / via hotkey / via failsafe.
 */
public enum LoopMode {
    ONCE,
    INFINITE
}
